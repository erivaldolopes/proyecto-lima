apiVersion: v1
kind: Service
metadata:
  name: php-apache-service
  namespace: clase8
  labels:
    app: php-apache
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
  selector:
    app: php-apache